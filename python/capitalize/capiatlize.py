# Using Python, have the function capitalize(str) take the str parameter being passed and capitalize
# the first letter of each word. Words will be separated by only one space.
# it should take string input from a user
# do not use the capitalize method


def capitalize():
	str = input("Enter your Word")
	

	for word in str:
		word.split()
		#print(y +str[1:])
		y = str[0].upper()
		z = y +str[1:]
		print(z)





capitalize()